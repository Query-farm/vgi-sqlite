// ============================================================================
// GENERATED by vgi.codegen.cpp_request_builders. DO NOT EDIT BY HAND.
//
// Generator: vgi-gen-cpp-request-builders v1
// Content hash: 64e0b7393794
//
// To regenerate:
//   scripts/regenerate_protocol.sh
// ============================================================================
//
// Emitted: 71 builders
// Skipped: 0 (Complex bucket — see vgi_rpc_types.cpp)
//
// ============================================================================

#pragma once

// Generated builders depend on helpers + the schema factories.
// Both live behind the same public header.
#include "wire/wire_builders.h"
#include "vgi/generated/vgi_protocol_schemas.hpp"

#include <cstdint>
#include <optional>
#include <string>
#include <utility>
#include <vector>

#include <arrow/api.h>

#include <stdexcept>

namespace vgi {
namespace generated {

// Origin: method 'aggregate_bind' params
inline std::shared_ptr<arrow::RecordBatch> BuildAggregateBindParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = AggregateBindParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildAggregateBindParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'aggregate_combine' params
inline std::shared_ptr<arrow::RecordBatch> BuildAggregateCombineParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = AggregateCombineParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildAggregateCombineParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'aggregate_destructor' params
inline std::shared_ptr<arrow::RecordBatch> BuildAggregateDestructorParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = AggregateDestructorParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildAggregateDestructorParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'aggregate_finalize' params
inline std::shared_ptr<arrow::RecordBatch> BuildAggregateFinalizeParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = AggregateFinalizeParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildAggregateFinalizeParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'aggregate_streaming_chunk' params
inline std::shared_ptr<arrow::RecordBatch> BuildAggregateStreamingChunkParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = AggregateStreamingChunkParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildAggregateStreamingChunkParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'aggregate_streaming_close' params
inline std::shared_ptr<arrow::RecordBatch> BuildAggregateStreamingCloseParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = AggregateStreamingCloseParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildAggregateStreamingCloseParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'aggregate_streaming_open' params
inline std::shared_ptr<arrow::RecordBatch> BuildAggregateStreamingOpenParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = AggregateStreamingOpenParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildAggregateStreamingOpenParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'aggregate_update' params
inline std::shared_ptr<arrow::RecordBatch> BuildAggregateUpdateParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = AggregateUpdateParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildAggregateUpdateParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'aggregate_window' params
inline std::shared_ptr<arrow::RecordBatch> BuildAggregateWindowParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = AggregateWindowParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildAggregateWindowParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'aggregate_window_batch' params
inline std::shared_ptr<arrow::RecordBatch> BuildAggregateWindowBatchParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = AggregateWindowBatchParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildAggregateWindowBatchParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'aggregate_window_destructor' params
inline std::shared_ptr<arrow::RecordBatch> BuildAggregateWindowDestructorParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = AggregateWindowDestructorParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildAggregateWindowDestructorParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'aggregate_window_init' params
inline std::shared_ptr<arrow::RecordBatch> BuildAggregateWindowInitParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = AggregateWindowInitParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildAggregateWindowInitParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'bind' params
inline std::shared_ptr<arrow::RecordBatch> BuildBindParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = BindParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildBindParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_attach' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogAttachParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = CatalogAttachParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogAttachParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_catalogs' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogCatalogsParams(
) {
	const auto &schema = CatalogCatalogsParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(0);

	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogCatalogsParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_copy_from_formats' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogCopyFromFormatsParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogCopyFromFormatsParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(2);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogCopyFromFormatsParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_create' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogCreateParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = CatalogCreateParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogCreateParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_detach' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogDetachParams(
	    const std::vector<uint8_t>& attach_opaque_data) {
	const auto &schema = CatalogDetachParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogDetachParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_drop' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogDropParams(
	    const std::string& name) {
	const auto &schema = CatalogDropParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildStringScalar(name));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogDropParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_index_create' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogIndexCreateParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = CatalogIndexCreateParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogIndexCreateParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_index_drop' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogIndexDropParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    bool ignore_not_found,
	    bool cascade,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogIndexDropParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildBoolScalar(cascade));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogIndexDropParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_index_get' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogIndexGetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogIndexGetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(4);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogIndexGetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_macro_create' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogMacroCreateParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = CatalogMacroCreateParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogMacroCreateParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_macro_drop' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogMacroDropParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    bool ignore_not_found,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogMacroDropParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(5);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogMacroDropParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_macro_get' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogMacroGetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogMacroGetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(4);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogMacroGetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_schema_contents_functions' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogSchemaContentsFunctionsParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& name,
	    const std::string& type,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogSchemaContentsFunctionsParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(4);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildEnumArray(type, {"TABLE", "VIEW", "SCALAR_FUNCTION", "TABLE_FUNCTION", "AGGREGATE_FUNCTION", "SCALAR_MACRO", "TABLE_MACRO", "INDEX"}));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogSchemaContentsFunctionsParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_schema_contents_indexes' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogSchemaContentsIndexesParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& name,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogSchemaContentsIndexesParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(3);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogSchemaContentsIndexesParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_schema_contents_macros' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogSchemaContentsMacrosParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& name,
	    const std::string& type,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogSchemaContentsMacrosParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(4);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildEnumArray(type, {"TABLE", "VIEW", "SCALAR_FUNCTION", "TABLE_FUNCTION", "AGGREGATE_FUNCTION", "SCALAR_MACRO", "TABLE_MACRO", "INDEX"}));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogSchemaContentsMacrosParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_schema_contents_tables' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogSchemaContentsTablesParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& name,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogSchemaContentsTablesParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(3);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogSchemaContentsTablesParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_schema_contents_views' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogSchemaContentsViewsParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& name,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogSchemaContentsViewsParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(3);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogSchemaContentsViewsParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_schema_create' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogSchemaCreateParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& name,
	    const std::string& on_conflict,
	    const std::optional<std::string>& comment,
	    const std::optional<std::vector<std::pair<std::string, std::string>>>& tags,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogSchemaCreateParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildEnumArray(on_conflict, {"ERROR", "IGNORE", "REPLACE"}));
	arrays.push_back(BuildOptionalStringScalar(comment));
	arrays.push_back(BuildOptionalStringMapScalar(tags));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogSchemaCreateParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_schema_drop' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogSchemaDropParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& name,
	    bool ignore_not_found,
	    bool cascade,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogSchemaDropParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(5);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildBoolScalar(cascade));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogSchemaDropParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_schema_get' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogSchemaGetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& name,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogSchemaGetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(3);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogSchemaGetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_schemas' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogSchemasParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogSchemasParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(2);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogSchemasParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_column_add' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableColumnAddParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::vector<uint8_t>& column_definition,
	    bool ignore_not_found,
	    bool if_column_not_exists,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableColumnAddParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(7);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildBinaryScalarRequired(column_definition));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildBoolScalar(if_column_not_exists));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableColumnAddParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_column_comment_set' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableColumnCommentSetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::string& column_name,
	    const std::optional<std::string>& comment,
	    bool ignore_not_found,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableColumnCommentSetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(7);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildStringScalar(column_name));
	arrays.push_back(BuildOptionalStringScalar(comment));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableColumnCommentSetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_column_default_drop' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableColumnDefaultDropParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::string& column_name,
	    bool ignore_not_found,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableColumnDefaultDropParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildStringScalar(column_name));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableColumnDefaultDropParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_column_default_set' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableColumnDefaultSetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::string& column_name,
	    const std::string& expression,
	    bool ignore_not_found,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableColumnDefaultSetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(7);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildStringScalar(column_name));
	arrays.push_back(BuildStringScalar(expression));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableColumnDefaultSetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_column_drop' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableColumnDropParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::string& column_name,
	    bool ignore_not_found,
	    bool if_column_exists,
	    bool cascade,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableColumnDropParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(8);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildStringScalar(column_name));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildBoolScalar(if_column_exists));
	arrays.push_back(BuildBoolScalar(cascade));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableColumnDropParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_column_rename' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableColumnRenameParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::string& column_name,
	    const std::string& new_column_name,
	    bool ignore_not_found,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableColumnRenameParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(7);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildStringScalar(column_name));
	arrays.push_back(BuildStringScalar(new_column_name));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableColumnRenameParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_column_statistics_get' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableColumnStatisticsGetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableColumnStatisticsGetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(4);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableColumnStatisticsGetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_column_type_change' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableColumnTypeChangeParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::vector<uint8_t>& column_definition,
	    const std::optional<std::string>& expression,
	    bool ignore_not_found,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableColumnTypeChangeParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(7);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildBinaryScalarRequired(column_definition));
	arrays.push_back(BuildOptionalStringScalar(expression));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableColumnTypeChangeParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_comment_set' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableCommentSetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::optional<std::string>& comment,
	    bool ignore_not_found,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableCommentSetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalStringScalar(comment));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableCommentSetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_create' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableCreateParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = CatalogTableCreateParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableCreateParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_delete_function_get' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableDeleteFunctionGetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableDeleteFunctionGetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(4);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableDeleteFunctionGetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_drop' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableDropParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    bool ignore_not_found,
	    bool cascade,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableDropParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildBoolScalar(cascade));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableDropParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_get' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableGetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::optional<std::string>& at_unit,
	    const std::optional<std::string>& at_value,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableGetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalStringScalar(at_unit));
	arrays.push_back(BuildOptionalStringScalar(at_value));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableGetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_insert_function_get' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableInsertFunctionGetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data,
	    const std::optional<std::string>& writable_branch_function_name) {
	const auto &schema = CatalogTableInsertFunctionGetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(5);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	arrays.push_back(BuildOptionalStringScalar(writable_branch_function_name));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableInsertFunctionGetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_not_null_drop' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableNotNullDropParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::string& column_name,
	    bool ignore_not_found,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableNotNullDropParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildStringScalar(column_name));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableNotNullDropParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_not_null_set' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableNotNullSetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::string& column_name,
	    bool ignore_not_found,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableNotNullSetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildStringScalar(column_name));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableNotNullSetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_rename' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableRenameParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::string& new_name,
	    bool ignore_not_found,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableRenameParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildStringScalar(new_name));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableRenameParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_scan_branches_get' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableScanBranchesGetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::optional<std::string>& at_unit,
	    const std::optional<std::string>& at_value,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableScanBranchesGetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalStringScalar(at_unit));
	arrays.push_back(BuildOptionalStringScalar(at_value));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableScanBranchesGetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_scan_function_get' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableScanFunctionGetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::optional<std::string>& at_unit,
	    const std::optional<std::string>& at_value,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableScanFunctionGetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalStringScalar(at_unit));
	arrays.push_back(BuildOptionalStringScalar(at_value));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableScanFunctionGetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_table_update_function_get' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTableUpdateFunctionGetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogTableUpdateFunctionGetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(4);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTableUpdateFunctionGetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_transaction_begin' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTransactionBeginParams(
	    const std::vector<uint8_t>& attach_opaque_data) {
	const auto &schema = CatalogTransactionBeginParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTransactionBeginParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_transaction_commit' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTransactionCommitParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::vector<uint8_t>& transaction_opaque_data) {
	const auto &schema = CatalogTransactionCommitParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(2);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildBinaryScalarRequired(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTransactionCommitParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_transaction_rollback' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogTransactionRollbackParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::vector<uint8_t>& transaction_opaque_data) {
	const auto &schema = CatalogTransactionRollbackParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(2);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildBinaryScalarRequired(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogTransactionRollbackParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_version' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogVersionParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogVersionParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(2);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogVersionParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_view_comment_set' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogViewCommentSetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::optional<std::string>& comment,
	    bool ignore_not_found,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogViewCommentSetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalStringScalar(comment));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogViewCommentSetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_view_create' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogViewCreateParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::string& definition,
	    const std::string& on_conflict,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogViewCreateParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildStringScalar(definition));
	arrays.push_back(BuildEnumArray(on_conflict, {"ERROR", "IGNORE", "REPLACE"}));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogViewCreateParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_view_drop' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogViewDropParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    bool ignore_not_found,
	    bool cascade,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogViewDropParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildBoolScalar(cascade));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogViewDropParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_view_get' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogViewGetParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogViewGetParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(4);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogViewGetParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'catalog_view_rename' params
inline std::shared_ptr<arrow::RecordBatch> BuildCatalogViewRenameParams(
	    const std::vector<uint8_t>& attach_opaque_data,
	    const std::string& schema_name,
	    const std::string& name,
	    const std::string& new_name,
	    bool ignore_not_found,
	    const std::optional<std::vector<uint8_t>>& transaction_opaque_data) {
	const auto &schema = CatalogViewRenameParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(6);
	arrays.push_back(BuildBinaryScalarRequired(attach_opaque_data));
	arrays.push_back(BuildStringScalar(schema_name));
	arrays.push_back(BuildStringScalar(name));
	arrays.push_back(BuildStringScalar(new_name));
	arrays.push_back(BuildBoolScalar(ignore_not_found));
	arrays.push_back(BuildOptionalBinaryScalar(transaction_opaque_data));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildCatalogViewRenameParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'init' params
inline std::shared_ptr<arrow::RecordBatch> BuildInitParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = InitParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildInitParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'table_buffering_combine' params
inline std::shared_ptr<arrow::RecordBatch> BuildTableBufferingCombineParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = TableBufferingCombineParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildTableBufferingCombineParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'table_buffering_destructor' params
inline std::shared_ptr<arrow::RecordBatch> BuildTableBufferingDestructorParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = TableBufferingDestructorParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildTableBufferingDestructorParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'table_buffering_process' params
inline std::shared_ptr<arrow::RecordBatch> BuildTableBufferingProcessParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = TableBufferingProcessParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildTableBufferingProcessParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'table_function_cardinality' params
inline std::shared_ptr<arrow::RecordBatch> BuildTableFunctionCardinalityParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = TableFunctionCardinalityParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildTableFunctionCardinalityParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'table_function_dynamic_to_string' params
inline std::shared_ptr<arrow::RecordBatch> BuildTableFunctionDynamicToStringParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = TableFunctionDynamicToStringParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildTableFunctionDynamicToStringParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'table_function_plan' params
inline std::shared_ptr<arrow::RecordBatch> BuildTableFunctionPlanParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = TableFunctionPlanParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildTableFunctionPlanParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

// Origin: method 'table_function_statistics' params
inline std::shared_ptr<arrow::RecordBatch> BuildTableFunctionStatisticsParams(
	    const std::vector<uint8_t>& request) {
	const auto &schema = TableFunctionStatisticsParamsSchema();
	static_assert(true, "compile-time anchor — runtime field-count assert below");
	std::vector<std::shared_ptr<arrow::Array>> arrays;
	arrays.reserve(1);
	arrays.push_back(BuildBinaryScalarRequired(request));
	if (arrays.size() != static_cast<size_t>(schema->num_fields())) {
		throw std::runtime_error("vgi codegen drift: BuildTableFunctionStatisticsParams produced " + std::to_string(arrays.size()) + " arrays but schema has " + std::to_string(schema->num_fields()) + " fields");
	}
	return arrow::RecordBatch::Make(schema, 1, arrays);
}

} // namespace generated
} // namespace vgi
